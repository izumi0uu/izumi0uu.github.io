<script type="module">
    import { localizePath, languageTag, availableLanguageTags, textDirection } from '@/paraglide/runtime'; // 确保路径正确

    window.paraglide = {
      localizePath,
      languageTag,
      availableLanguageTags,
      textDirection
    };

    // 可选：如果 i18nScript 依赖于 Paraglide 运行时已准备好，
    // 可以派发一个自定义事件，让 i18nScript 监听这个事件后再执行。
    // document.dispatchEvent(new CustomEvent('paraglideRuntimeLoaded'));
  </script>