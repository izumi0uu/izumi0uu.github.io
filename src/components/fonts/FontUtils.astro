---
/**
 * Font utility component
 * Provide font-related utility classes and styles
 * Integrate with the font system in foundation.css
 */
---

<style is:global>
  /* ===== Language-specific font classes ===== */

  /* Prefer stable system CJK fonts to avoid late network font swaps. */
  .zh-only {
    font-family:
      "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC",
      "Source Han Sans SC", ui-sans-serif, system-ui, sans-serif;
  }

  /* Pure English elements (FontLoader provides the Inter face). */
  .en-only {
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-variation-settings: "opsz" auto;
  }

  /* ===== Semantic font classes (use CSS variables) ===== */

  /* Mixed font stack (default) */
  .font-mixed,
  .font-sans {
    font-family: var(--font-family-sans);
  }

  /* Monospace font (FontLoader binds code, pre, kbd, .font-code) */
  .font-mono {
    font-family: var(--font-family-mono);
  }

  /* ===== Advanced font features ===== */

  /* Inter OpenType features optimization */
  .font-inter-optimized {
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-feature-settings: "cv02", "cv03", "cv04", "cv11";
    font-variation-settings: "opsz" auto;
  }

  /* code font optimization (disable ligatures) */
  .font-mono-optimized {
    font-family: var(--font-family-mono);
    font-feature-settings: "liga" 0;
    font-variant-ligatures: none;
  }
</style>

<!--
Usage guide:
- .zh-only: Prefer stable system CJK fonts
- .en-only: Pure English elements (FontLoader already binds the Inter face)
- .font-mixed / .font-sans: Mixed Chinese and English (default)
- .font-mono: Monospace font (FontLoader binds code, pre, kbd, .font-code)
- .font-inter-optimized: Inter advanced features
- .font-mono-optimized: Code font optimization
-->
