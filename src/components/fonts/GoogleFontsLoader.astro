---
/**
 * Chinese font loader
 * Load Noto Sans SC font from Google Fonts, optimize performance
 * Only load Chinese font, local font (English and code) is handled by FontLoader component
 * Keep the same font system as foundation.css
 */
---

<Fragment>
  {/* preconnect the Google Fonts domain to optimize the loading performance */}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  
  {/* load the Noto Sans SC Chinese font */}
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&display=swap" rel="stylesheet">
  
  {/* apply the selector style to the Chinese font */}
  <style is:global>
    /* full Chinese font class */
    .zh-only,
    .font-zh {
      font-family: "Noto Sans SC", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    
    {/* set the default font stack (mixed Chinese and English) */}
    html {
      font-family: var(--font-family-sans, "Inter", "Noto Sans SC", ui-sans-serif, system-ui, sans-serif);
      /* optimize font rendering */
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      text-rendering: optimizeLegibility;
    }

  </style>
</Fragment> 