---
/**
 * Chinese font loader component
 * Use Google Fonts to load the Noto Sans SC font, optimize performance
 * Only load the Chinese font, local fonts (English and code) are handled by the FontLoader component
 */
---

<Fragment>
  {/* preconnect the Google Fonts domain to optimize the loading performance */}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  
  {/* load the Noto Sans SC Chinese font */}
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&display=swap" rel="stylesheet">
  
  {/* apply the selector style to the Chinese font */}
  <style is:global>
    .zh-only {
      font-family: "Noto Sans SC", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    
    {/* set the default font stack (mixed Chinese and English) */}
    html {
      font-family:
        "Inter",
        "Noto Sans SC",
        system-ui,
        -apple-system,
        BlinkMacSystemFont,
        "Segoe UI",
        sans-serif;
    }
  </style>
</Fragment> 