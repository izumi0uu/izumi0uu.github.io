---
/*
  This script is used to initialize the i18n redirect.
  It is a ES module, so that we can import ParaglideJS runtime functions.
  It is also a module script, so that we can use the initializeRedirect.
*/
---
<script type="module">
  import { initializeRedirect } from '../scripts/i18n-initializer.ts';

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initializeRedirect);
  } else {
    initializeRedirect();
  }
</script>