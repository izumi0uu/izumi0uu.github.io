---
/*
* depends on user's browser language setting
 */
import * as i18nConstants from "@/config/i18n";


---

<script is:inline define:vars={{ supportedLocales: i18nConstants.SUPPORTED_LOCALES, defaultLocale: i18nConstants.DEFAULT_LOCALE, localeLabels: i18nConstants.LOCALE_LABELS }}>
  
</script>